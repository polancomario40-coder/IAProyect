window.env = {
  // Cambia esta URL por la dirección real donde publicaste tu API backend en el IIS.
  // Ejemplo: "https://api-seguridad.sade.com.do/api" o "https://tudominio.com/api"
  API_URL: "https://tudominio.com/api"
};
